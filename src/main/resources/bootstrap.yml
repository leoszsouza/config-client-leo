spring:
  application:
    name: config-client-leo
  profiles:
    active: prod
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: "*"